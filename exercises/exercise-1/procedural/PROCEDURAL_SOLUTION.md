# Procedural Solution

The solution follows a sequential approach using explicit iteration
and a mutable accumulator.

1. Receive the collection of integers.
2. Initialize an accumulator with the value zero.
3. Iterate through each element of the collection.
4. Add the current element to the accumulator.
5. Continue until all elements have been processed.
6. Return the accumulated value as the result.