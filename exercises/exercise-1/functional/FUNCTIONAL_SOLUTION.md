# Functional Solution

The solution represents the sum as a function over a list of integers.

1. Receive a list of integers.
2. If the list is empty, return zero.
3. Separate the first element from the remaining elements.
4. Recursively calculate the sum of the remaining elements.
5. Add the first element to the result of the recursive calculation.
6. Return the resulting value.